# hubspot-command — $49

Create a Private App in Settings → Integrations → Private Apps. Grant `crm.objects.contacts.*`, `crm.objects.deals.*`, `crm.objects.notes.*`, `crm.objects.tasks.*`. Paste the access token.

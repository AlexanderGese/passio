# linkedin-command — $49

Bring your own LinkedIn app (scopes: `w_member_social r_liteprofile r_emailaddress`), 3-legged OAuth yourself once, paste the access_token + your `urn:li:person:<id>` in Settings.

Tools: `me`, `share_text`, `share_article`, `comment`, `like`, `feed`, `message`, `connections_count`, `autopilot_*`. All agent-callable.

Autopilot default: off + dry-run + 1/day cap + 720-min gap. Keep dry-run on until drafts consistently match your voice.

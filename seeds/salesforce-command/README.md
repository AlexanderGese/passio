# salesforce-command — $79

Create a Connected App in SFDC (Setup → App Manager → New Connected App), enable OAuth, scopes `api` + `refresh_token offline_access`. Use the CLI or a one-time OAuth flow to mint an access token + get your org's `instance_url`. Paste both.

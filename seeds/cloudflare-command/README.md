# cloudflare-command — $49

API token at https://dash.cloudflare.com/profile/api-tokens (custom template: Zone DNS edit + Workers Script edit + KV Storage edit for whichever zones/accounts). Paste token + account id in Settings.

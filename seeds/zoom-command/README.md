# zoom-command — $39

Create a Server-to-Server OAuth app in Zoom Marketplace. Scopes: `meeting:read, meeting:write, recording:read, user:read`. The seed uses its access token directly — refresh handling is on you.

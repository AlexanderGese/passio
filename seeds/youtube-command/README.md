# youtube-command — $59

OAuth via Google Cloud (scope `youtube.force-ssl`). Paste access token. Upload is resumable over `https://www.googleapis.com/upload/youtube/v3/videos?uploadType=resumable` — implement via chat agent tool wrapping if you need it; this seed ships metadata + comment + playlist surface.

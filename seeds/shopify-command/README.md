# shopify-command — $79

Create a custom app in Shopify Admin (Apps → Develop apps), grant Admin API access (orders/products/customers/inventory/fulfillments), install it, grab the `shpat_…` token. Paste + your shop subdomain in Settings.

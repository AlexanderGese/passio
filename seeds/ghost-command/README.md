# ghost-command — $29

Create a Custom Integration in Ghost Admin → Integrations, copy the **Admin API Key** (format `id:secret`), paste + your site URL in Settings. The seed mints a short-lived JWT for each request — no token exposure in transit.
